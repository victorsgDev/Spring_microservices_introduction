server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/victorsgDev/microservices_introduction.git
          search-paths: config-data
  application:
    name: config-service
